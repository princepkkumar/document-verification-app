import React from 'react';

// This is a simple visual component for the loading spinner.
// Its appearance is controlled by the CSS we will add in the next step.
const Spinner = () => {
  return <div className="loading-spinner"></div>;
};

export default Spinner;